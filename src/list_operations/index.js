const { filter } = require("./filter")
const { map } = require("./map")
const { reduce } = require("./reduce")
const { reduceRight } = require("./reduce-right")

module.exports = { filter, map, reduce, reduceRight }
